# auto_sy 项目梳理

## 1. 项目定位

这个项目是一个基于 Python 和 PySide6 的 ERP 自动化工具，核心用途是通过接口调用批量执行业务流程，包括销售订单、采购订单和生产工单相关的全流程操作。

项目结构可以概括为：

- 界面入口：main.py
- 接口基础层：baseApi/
- 配置与工具层：common/、config/
- 业务编排层：src/configuration/configuration.py
- 具体业务模块：src/ 下各子目录
- 数据修正工具：src/database_manipulate/database_manipulate.py
- 时间辅助工具：src/time/time_utils.py

## 2. 启动入口与执行链路

### 2.1 程序入口

入口文件是 main.py。启动后会创建一个 ErpGUI 窗口，用户在界面中输入：

- 登录用户
- MRP 方案名称和 ID
- 生产工单号
- 仓库名
- 质检人
- 时间偏移天数
- 销售订单 Excel 路径
- 采购订单 Excel 路径

然后从下拉框中选择三种方法之一：

- 方法一：销售订单 -> MRP -> 生产计划 / 采购计划
- 方法二：采购订单 -> 到货 / 检验 / 入库
- 方法三：生产工单 -> 领料 -> 投产 -> 工序流转 / 检验 / 入库

### 2.2 主调用顺序

启动后核心链路如下：

1. main.py 创建 AllApi
2. main.py 创建 Configuration
3. 用户选择方法并点击执行
4. 先调用 AllApi.send_login() 完成登录并写入 token
5. 再调用 Configuration.run_one()、run_two() 或 run_three() 执行业务流程

## 3. 模块分层

### 3.1 基础通信层

#### baseApi/base_api.py

这个模块是整个项目最底层的接口封装，职责包括：

- 读取配置文件和 token 文件
- 调用统一请求工具 RunMethod
- 登录接口 send_login
- 通用请求接口：send_get_direct、send_post_direct、send_putJson 等
- 数据库连接 get_conn

它既负责接口调用，也负责数据库连接，因此是整个项目的核心基础设施。

### 3.2 通用工具层

#### common/file_path.py

负责定位配置文件路径：

- get_config_path()：返回 config.yml 路径
- get_token_path()：返回 token.yml 路径

#### common/read_yaml.py

负责 YAML 的读写：

- ReadYaml()：读取 YAML 并返回字典
- write_token()：把登录接口返回的 token 写入 token.yml

#### common/write_yaml.py

提供保留注释和格式的 YAML 更新方法 update_yaml_pyyaml()，属于辅助能力。

### 3.3 配置层

#### config/config.yml

保存接口地址、用户信息、数据库连接等静态配置。

#### config/token.yml

保存登录后写入的 token，是运行时凭证文件。

### 3.4 业务编排层

#### src/configuration/configuration.py

这是当前项目的总编排器，负责把各个零散业务模块串成完整流程。

它在初始化时创建了大量业务对象，例如：

- 销售订单读取器
- 采购订单读取器
- MRP 计算器
- 到货、检验、入库模块
- 生产领料、投产、派工、上线、报工、转移、入库模块
- 数据库时间修正模块

它定义了三个主入口：

- run_one()：销售驱动流程
- run_two()：采购驱动流程
- run_three()：生产驱动流程

### 3.5 数据输入层

#### src/read_xlsx/read_sales_xlsx.py

负责读取销售订单 Excel，并转换成销售订单接口需要的 payload。

主要做了这些事情：

- 读取表格
- 把中文列名映射成接口字段名
- 查询客户 ID、销售员 ID、物料详情
- 计算含税单价、不含税单价、金额、税额
- 按销售单号分组输出 payload

#### src/read_xlsx/read_buy_xlsx.py

负责读取采购订单 Excel，并转换成采购订单接口需要的 payload。

主要做了这些事情：

- 读取表格
- 把中文列名映射成接口字段名
- 查询供应商 ID、采购员 ID、物料详情
- 计算税价、税额、总价
- 按采购单号分组输出 payload

## 4. 三条主业务链路

### 4.1 方法一：销售订单链路

链路如下：

1. 从 Excel 读取销售订单
2. 调用 SaleOrder.sale_order_add() 新增销售订单并审批
3. 调用 MRPCalculation.mrp_calculation() 执行 MRP 运算
4. 调用 MRPCalculation.production_and_buy_plan() 生成生产计划和采购计划

涉及模块：

- src/read_xlsx/read_sales_xlsx.py
- src/sale_order/sale_order.py
- src/mrp/mrp_calculate.py

### 4.2 方法二：采购订单链路

链路如下：

1. 从 Excel 读取采购订单
2. 调用 BuyOrderNew.buy_order_add() 新增并审批采购订单
3. 调用 IsExemptInspection.item_code_get() 判断是否需要检验
4. 如果需要检验：
   - BuyArrival.buy_arrival_add() 创建到货单并审批
   - BuyInspection.buy_inspection_add() 创建采购检验单并审批
   - BuyInventory.buy_inventory_add_by_inspection() 按检验单入库
   - 对免检物料再走 BuyInventory.buy_inventory_add_by_order() 直接入库
5. 如果全部免检：
   - 直接调用 BuyInventory.buy_inventory_add_by_order() 入库

涉及模块：

- src/read_xlsx/read_buy_xlsx.py
- src/buy_order/buy_order_new.py
- src/is_exempt_inspection/is_exempt_inspection.py
- src/buy_arrival/buy_arrival.py
- src/buy_inspection/buy_inspection.py
- src/buy_inventory/buy_inventory.py

### 4.3 方法三：生产工单链路

这个链路分成两条分支：有工序报工和无工序报工。

#### 有工序报工分支

1. ProductionRequisition.production_requisition_add() 生产领料并审批
2. ProcessCommission.process_commission() 工单投产
3. ProcessCommission.has_process_dispatch() 判断系统是否启用工序派工
4. 如果需要派工：
   - ProcessDispatch.process_dispatch_add() 做工序派工
   - ProcessOnline.process_online() 做工序上线
   - ProcessReporting.process_reporting_add() 做工序报工
   - ProcessReporting.has_process_inspection() 判断工序是否免检
   - 若不免检，ProcessInspection.process_inspection_add() 创建工序检验单
   - 如果不是末工序，ProcessTransfer.process_transfer() 做工序转移
   - 如果是末工序，ProcessInventory.process_inventory_add() 做工序入库

#### 无工序报工分支

1. ProductionRequisition.production_requisition_add() 生产领料并审批
2. ProductionRequisition.item_code_get_by_product_code() 判断成品是否免检
3. 如果不免检：
   - ProductionSubmission.production_submission_add() 创建产品送检单
   - ProductionInspection.production_inspection_add() 创建生产检验单
   - ProductionInventory.production_inventory_add_by_inspection() 按检验单做产品入库
4. 如果免检：
   - ProductionInventory.production_inventory_add_by_production() 直接按生产单入库

涉及模块：

- src/production_requisition/production_requisition.py
- src/process_commission/process_commission.py
- src/process_dispatch/process_dispatch.py
- src/process_online/process_online.py
- src/process_reporting/process_reporting.py
- src/process_inspection/process_inspection.py
- src/process_transfer/pocess_transfer.py
- src/process_inventory/process_inventory.py
- src/production_submission/production_submission.py
- src/production_inspection/production_inspection.py
- src/production_inventory/production_inventory.py

## 5. 模块对应关系

### 5.1 入口到编排器

- main.py 负责 UI 和参数采集
- main.py 持有 AllApi 与 Configuration
- Configuration 负责实际的流程拼装

### 5.2 编排器到业务模块

Configuration 中的对象与职责大致对应如下：

- reader_sales -> 销售 Excel 读取
- sale_order -> 销售订单创建与审批
- mrp_cal -> MRP 运算和计划生成
- reader_buy -> 采购 Excel 读取
- buy_order_new -> 采购订单创建与审批
- buy_arrival -> 到货单创建与审批
- buy_inspection -> 采购检验单创建与审批
- buy_inventory -> 采购入库
- is_exempt_inspection -> 免检判断
- production_requisition -> 生产领料
- process_commission -> 工单投产
- process_dispatch -> 工序派工
- process_online -> 工序上线
- process_reporting -> 工序报工
- process_inspection -> 工序检验
- process_transfer -> 工序转移
- process_inventory -> 工序入库
- production_submission -> 产品送检
- production_inspection -> 生产检验
- production_inventory -> 产品入库
- database_manipulate -> 调整数据库时间字段

### 5.3 业务模块对基础层的依赖

大多数业务模块都依赖 AllApi，通过它完成：

- 获取自动编号
- 查询基础资料
- 查询单据详情
- 发起新增 / 审批接口

其中少数模块还会直接依赖数据库：

- src/database_manipulate/database_manipulate.py

## 6. 关键辅助机制

### 6.1 token 流程

登录成功后：

1. AllApi.send_login() 调用登录接口
2. write_token() 把 token 写到 config/token.yml
3. 后续请求通过 token 文件读取凭证并放入请求头

### 6.2 时间模拟流程

`src/time/time_utils.py` 和 `src/database_manipulate/database_manipulate.py` 配合使用，目的不是简单改时间，而是让审批、派工、报工、检验等业务记录更像真实工作时间下的流转结果。

## 7. 主要业务对象功能摘要

### 销售相关

- src/read_xlsx/read_sales_xlsx.py：销售 Excel 转 payload
- src/sale_order/sale_order.py：销售订单新增与审批
- src/mrp/mrp_calculate.py：MRP 计算与计划生成

### 采购相关

- src/read_xlsx/read_buy_xlsx.py：采购 Excel 转 payload
- src/buy_order/buy_order_new.py：采购订单新增与审批
- src/is_exempt_inspection/is_exempt_inspection.py：采购免检判断
- src/buy_arrival/buy_arrival.py：采购到货
- src/buy_inspection/buy_inspection.py：采购检验
- src/buy_inventory/buy_inventory.py：采购入库

### 生产相关

- src/production_requisition/production_requisition.py：生产领料
- src/process_commission/process_commission.py：工单投产
- src/process_dispatch/process_dispatch.py：工序派工
- src/process_online/process_online.py：工序上线
- src/process_reporting/process_reporting.py：工序报工
- src/process_inspection/process_inspection.py：工序检验
- src/process_transfer/pocess_transfer.py：工序转移
- src/process_inventory/process_inventory.py：工序入库
- src/production_submission/production_submission.py：产品送检
- src/production_inspection/production_inspection.py：生产检验
- src/production_inventory/production_inventory.py：产品入库

## 8. 结构问题与遗留点

仓库中可以明确看到一些结构和命名问题：

- _init_.py 不是标准的 Python 包初始化文件名，标准写法应是 __init__.py
- bulid.bat 存在拼写问题，疑似应为 build.bat
- src/process_transfer/pocess_transfer.py 文件名拼写错误，疑似应为 process_transfer.py
- src/configuration/configuration.py 里有重复初始化 ProductionRequisition 的情况
- 部分模块有硬编码路径和 sys.path.append 的处理方式，维护性一般

## 9. 架构观察

1. 这个项目是典型的流程编排型接口自动化，不是通用业务平台。
2. Configuration 是绝对中心节点，耦合度很高，后续维护时要重点关注。
3. Excel 导入模块是多个流程的上游入口，字段映射和基础资料查询非常关键。
4. DatabaseManipulate 的存在说明它不仅是在调用接口，还会直接改库以模拟业务时间。
5. 当前代码更偏向“可跑通流程”，而不是“结构整洁、抽象统一”，因此后续整理时建议先梳理主链路，再收敛公共能力。

## 10. 适合后续整理的方向

- 先整理主链路，再整理旧脚本和备用脚本
- 统一包初始化文件名
- 统一命名规范，修正拼写错误
- 把重复的用户查询、自动编号查询、审批模板封装成公共方法
- 逐步降低 Configuration 的耦合度

## 11. 详细模块索引

这一部分用于接手时快速定位代码。

### 11.1 baseApi

- baseApi/base_api.py
   - AllApi：接口调用、登录、token 读取、数据库连接
- baseApi/run_method.py
   - RunMethod：真正发起 HTTP 请求的底层封装

### 11.2 common

- common/file_path.py
   - 配置文件和 token 文件路径定位
- common/read_yaml.py
   - YAML 读取、token 写入
- common/write_yaml.py
   - 保留格式的 YAML 更新

### 11.3 src/read_xlsx

- read_sales_xlsx.py
   - Excel 销售订单转接口 payload
- read_buy_xlsx.py
   - Excel 采购订单转接口 payload

### 11.4 src/configuration

- configuration.py
   - 主编排器，负责把所有业务模块串成流程

### 11.5 src/sale_order

- sale_order.py
   - 当前使用的销售订单实现
- sale_order_old.py
   - 旧实现，保留备用痕迹

### 11.6 src/mrp

- mrp_calculate.py
   - MRP 运算、生产计划和采购计划生成

### 11.7 src/buy_order

- buy_order_new.py
   - 当前使用的采购订单创建实现
- buy_order.py
   - 两个旧版变体，偏示例或历史实现
- buy_order_choose.py
   - 旧实现，通常用于选单创建采购订单
- buy_order_by_erp.py
   - 由 MRP 结果生成采购订单的脚本

### 11.8 src/buy_arrival / buy_inspection / buy_inventory

- buy_arrival.py
   - 到货单
- buy_inspection.py
   - 采购检验单
- buy_inventory.py
   - 采购入库
- testing_inventory.py
   - 采购入库相关的测试或备用实现

### 11.9 src/is_exempt_inspection

- is_exempt_inspection.py
   - 采购物料免检判断

### 11.10 src/production_requisition

- production_requisition.py
   - 生产领料

### 11.11 src/process_* 系列

- process_commission.py
   - 工单投产
- process_dispatch.py
   - 工序派工
- process_online.py
   - 工序上线
- process_reporting.py
   - 工序报工
- process_inspection.py
   - 工序检验
- pocess_transfer.py
   - 工序转移，文件名拼写有误
- process_inventory.py
   - 工序入库

### 11.12 src/production_* 系列

- production_submission.py
   - 产品送检单
- production_inspection.py
   - 生产检验单
- production_inventory.py
   - 产品入库

### 11.13 其他

- sale_out.py
   - 销售出库脚本，当前不在 main.py 主链路中
- time_utils.py
   - 工作时间、随机延时、合法时间推进
- database_manipulate.py
   - 直接修改数据库时间字段

## 12. 调用关系速查

### 12.1 销售链路速查

```text
main.py
   -> Configuration.run_one()
      -> ReadSalesXlsx.read_sales_xlsx()
      -> SaleOrder.sale_order_add()
      -> MRPCalculation.mrp_calculation()
      -> MRPCalculation.production_and_buy_plan()
```

### 12.2 采购链路速查

```text
main.py
   -> Configuration.run_two()
      -> ReadBuyXlsx.read_buy_xlsx()
      -> BuyOrderNew.buy_order_add()
      -> IsExemptInspection.item_code_get()
      -> BuyArrival.buy_arrival_add()
      -> BuyInspection.buy_inspection_add()
      -> BuyInventory.buy_inventory_add_by_inspection()
      -> BuyInventory.buy_inventory_add_by_order()
```

### 12.3 生产链路速查

```text
main.py
   -> Configuration.run_three()
      -> ProductionRequisition.production_requisition_add()
      -> ProcessCommission.process_commission()
      -> ProcessDispatch.process_dispatch_add()
      -> ProcessOnline.process_online()
      -> ProcessReporting.process_reporting_add()
      -> ProcessInspection.process_inspection_add()
      -> ProcessTransfer.process_transfer()
      -> ProcessInventory.process_inventory_add()
      -> ProductionSubmission.production_submission_add()
      -> ProductionInspection.production_inspection_add()
      -> ProductionInventory.production_inventory_add_by_inspection()
      -> ProductionInventory.production_inventory_add_by_production()
```

## 13. 关键返回值说明

为了后续排查问题，下面列出一些常见方法的返回值语义。

- ReadSalesXlsx.read_sales_xlsx()
   - 返回销售订单 payload 列表
- ReadBuyXlsx.read_buy_xlsx()
   - 返回采购订单 payload 列表
- SaleOrder.sale_order_add()
   - 返回 businessId
- MRPCalculation.mrp_calculation()
   - 返回 key
- MRPCalculation.production_and_buy_plan()
   - 返回生成计划接口响应
- BuyOrderNew.buy_order_add()
   - 直接完成新增和审批，通常不显式返回值
- BuyArrival.buy_arrival_add()
   - 返回 businessId 和 receiptsCode
- BuyInspection.buy_inspection_add()
   - 返回 inspectionCodes 列表
- BuyInventory.buy_inventory_add_by_order()
   - 返回 businessId
- BuyInventory.buy_inventory_add_by_inspection()
   - 返回 businessId
- ProductionRequisition.production_requisition_add()
   - 返回 businessId
- ProcessCommission.process_commission()
   - 返回 item_codes 列表
- ProcessOnline.process_online()
   - 返回上线单据 id
- ProcessReporting.process_reporting_add()
   - 返回 processCode
- ProcessInspection.process_inspection_add()
   - 返回 inspectionCode
- ProductionSubmission.production_submission_add()
   - 返回 submissionCode
- ProductionInspection.production_inspection_add()
   - 返回 inspectionCodes 列表
- ProductionInventory.production_inventory_add_by_production()
   - 完成入库审批，通常不显式返回值
- ProductionInventory.production_inventory_add_by_inspection()
   - 完成入库审批，通常不显式返回值

## 14. 接手时最先看什么

如果你是第一次接手这个项目，建议按下面顺序看：

1. main.py，先看界面输入和三条主链路入口
2. src/configuration/configuration.py，理解流程编排
3. baseApi/base_api.py，理解登录、token、请求和数据库连接
4. src/read_xlsx/read_sales_xlsx.py 和 src/read_xlsx/read_buy_xlsx.py，理解数据如何进入系统
5. 再看各业务模块，按销售、采购、生产三条线逐个展开
